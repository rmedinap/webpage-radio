%h1 Listing contactos

%table
  %tr
    %th Nombre
    %th Email
    %th Mensaje
    %th
    %th
    %th

  - @contactos.each do |contacto|
    %tr
      %td= contacto.nombre
      %td= contacto.email
      %td= contacto.mensaje
      %td= link_to 'Show', contacto
      %td= link_to 'Edit', edit_contacto_path(contacto)
      %td= link_to 'Destroy', contacto, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Contacto', new_contacto_path
