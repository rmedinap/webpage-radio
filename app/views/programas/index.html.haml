
= render 'scripts/codaslider'
= render 'programas_menu'

.region
  %h2.title
    = page_title! 'Programación'

  .administration.head
    = link_to 'New Programa', new_dia_path

  #week-days.coda-slider
    - @dias.each do |dia|
      %div
        %h2.dia
          = link_to dia.nombre, dia
        -# dia.programas.each do |programa|
          = #programa.titulo





-#
  - @programas.each do |programa|
    %article
      %header
        %h3
          = programa.titulo
      .inner
        = paragraph(programa.contenido)
      .administration.iteration
        = link_to 'Editar', edit_programa_path(programa), :class => 'button edit'
        = link_to 'Eliminar', programa, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'button delete'






